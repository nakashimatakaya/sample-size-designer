# R/glossary.R
# 用語解説（ツールチップ + 用語集 + デザイン選び方 + 結果読み解きヒント）。
# 臨床研究者（非統計家）が読む想定のため「です・ます調」で柔らかく書く。

# =========================================================================
# ツールチップ用の短い解説（2〜3 行）
# =========================================================================
tooltip_texts <- list(
  alpha = "偶然の差を「本物の差」と誤って判定する確率の上限です。通常は 0.05（5%）を使います。値を小さくするほど誤判定は減りますが、必要症例数は増えます。",
  power = "想定した差が本当に存在するとき、それを統計的に検出できる確率です。通常は 0.80（80%）を目標にします。",
  effect_size = "群間の違いの大きさを「ばらつき」で割って標準化した指標です。小 = 0.2、中 = 0.5、大 = 0.8 が目安です（Cohen 1988）。",
  dropout = "最終的な脱落割合です。試験に組み入れたあと、追跡不能や同意撤回などで解析対象から外れる人の割合を過去の類似研究から見積もります。",
  sd = "測定値のばらつきの大きさです。過去の研究や予備データから見積もります。",
  margin = "新治療が既存治療より「この程度までなら劣っていても臨床的に許容できる」と事前に定める差の大きさです。臨床的・歴史的根拠に基づいて設定します。",
  p_A = "介入群でアウトカム（例: イベント発生）が起こる確率です。過去の研究から見積もります。",
  p_B = "対照群でアウトカム（例: イベント発生）が起こる確率です。過去の研究から見積もります。",
  half_width = "推定値の精度の目標です。「平均値 ± E」または「割合 ± E」の幅で推定したいときに指定します。小さくするほど多くの症例が必要です。",
  conf_level = "信頼区間の信頼水準です。95% が最も一般的で、99% を選ぶとより厳しい（幅の広い）区間になり、必要症例数は増えます。",
  mean_A = "介入群のアウトカム（連続量）の平均値の想定です。",
  mean_B = "対照群のアウトカム（連続量）の平均値の想定です。",
  diff = "介入群と対照群の平均値の差（Δ = 介入群 − 対照群）の想定です。",
  diff_mean = "対応のあるペアの差の平均（例: 介入後 − 介入前）の想定です。",
  sd_A = "介入群のアウトカム（連続量）の標準偏差の想定です。",
  sd_B = "対照群のアウトカム（連続量）の標準偏差の想定です。",
  sd_diff = "対応のあるペアの差そのものの標準偏差です。",
  r = paste(
    "同じ被験者における測定 1 と測定 2 の関連の強さです。",
    "r = 0 なら 2 時点は独立、r = 1 なら完全に一致します。",
    "過去の類似研究や予備データから推定します。",
    "r が大きいほど差のばらつきが小さくなり、対応のある試験の効率が上がります。",
    "通常は 0.3〜0.7 の範囲です。",
    sep = "\n"
  ),
  mean_1 = "介入前（ベースライン、測定 1 時点目）のアウトカムの平均値の想定です。",
  mean_2 = "介入後（測定 2 時点目）のアウトカムの平均値の想定です。",
  sd_1 = "介入前のアウトカムの標準偏差の想定です。",
  sd_2 = "介入後のアウトカムの標準偏差の想定です。"
)

# =========================================================================
# 用語集（やや長めの解説）
# =========================================================================
# 各項目: list(title, body)
glossary_entries <- list(
  list(
    title = "有意水準 α",
    body = paste(
      "偶然の差を「本物の差」と誤って判定する確率の上限です。",
      "通常は 0.05（5%）を使います。",
      "α = 0.05 とは、本当は差がないのに「差がある」と判定してしまう確率を",
      "5% 以下に抑える、という意味です。",
      "値を小さくするほど誤判定は減りますが、同じ差を検出するのに必要な",
      "症例数は増えます。非劣性試験では片側 α = 0.025 をよく使います。",
      sep = "\n"
    )
  ),
  list(
    title = "検出力 (1 − β)",
    body = paste(
      "想定した差が本当にあった場合に、その試験で「統計的に有意」と",
      "判定できる確率です。",
      "検出力 0.80 とは、本物の差を見逃す確率が 20% という意味です。",
      "検出力を 0.90 に上げれば見逃しは 10% に減りますが、",
      "必要症例数が約 30% 増えます。",
      "目標検出力は通常 0.80 ですが、重要な試験や見逃しが致命的な",
      "場合は 0.90 を使うこともあります。",
      sep = "\n"
    )
  ),
  list(
    title = "効果量（Cohen's d、Cohen's h）",
    body = paste(
      "群間の違いの大きさを「ばらつき」で標準化した指標です。",
      "連続量では d = |平均の差| ÷ SD、二値では h = 2·asin(√p_A) − 2·asin(√p_B)",
      "で計算されます。",
      "d の目安は Cohen (1988) によると、小 = 0.2、中 = 0.5、大 = 0.8 です。",
      "臨床的な意味づけはこの目安だけに頼らず、「この差が臨床現場で",
      "本当に意味があるか」という観点から議論してください。",
      sep = "\n"
    )
  ),
  list(
    title = "最終的な脱落割合（dropout rate）",
    body = paste(
      "試験に組み入れたあと、追跡不能・同意撤回・治療中断などで",
      "解析対象から外れる割合（最終的に評価できない人の割合）です。",
      "たとえば最終的な脱落割合 10% を見込むなら、解析対象で 100 例必要なとき",
      "登録時点では約 112 例（= ⌈100 / 0.90⌉）をリクルートします。",
      "過去の類似研究から見積もるのが一般的です。",
      "実際の脱落が想定を超える場合は、追加登録や再計算が必要になります。",
      sep = "\n"
    )
  ),
  list(
    title = "標準偏差 SD",
    body = paste(
      "測定値のばらつきの大きさです。",
      "過去の研究や予備データから見積もるのが基本です。",
      "SD を大きく見積もるほど必要症例数は増え、小さく見積もるほど",
      "減りますが、実際より小さく見積もると検出力が不足するため、",
      "やや大きめに見積もる（保守的に）のが安全です。",
      sep = "\n"
    )
  ),
  list(
    title = "非劣性マージン M",
    body = paste(
      "新治療が既存治療より「この程度までなら劣っていても",
      "臨床的に許容できる」と事前に定める差の大きさです。",
      "連続量なら平均差の単位、二値ならリスク差の単位で指定します。",
      "臨床的な妥当性と、過去試験で既存治療が示したプラセボ差の",
      "一定割合（例えば半分）を上限とする、などの規制的な観点から",
      "事前に決めます。試験後に変更するのは不適切です。",
      sep = "\n"
    )
  ),
  list(
    title = "割合 p_A, p_B",
    body = paste(
      "各群でアウトカム（例: イベント発生、治療成功）が起こる確率です。",
      "過去の研究や登録データから見積もります。",
      "p_A と p_B の差の「絶対的な大きさ」だけでなく、",
      "p の水準自体（例: 0.01 vs 0.02 と 0.50 vs 0.51）で",
      "必要症例数は大きく変わる点に注意してください。",
      sep = "\n"
    )
  ),
  list(
    title = "相関係数 r（対応のある t 検定）",
    body = paste(
      "同じ被験者における測定 1（例: 治療前）と測定 2（例: 治療後）の",
      "関連の強さを表す指標です。−1 から 1 の値を取ります。",
      "r = 0 なら 2 時点は独立、r = 1 なら完全に一致します。",
      "差の SD は sqrt(sd_1^2 + sd_2^2 − 2·r·sd_1·sd_2) で計算されるため、",
      "r が大きいほど差のばらつきが小さくなり、対応のある試験の",
      "効率が上がります。",
      "臨床研究では 0.3〜0.7 の範囲がよく使われますが、",
      "過去の類似研究や予備データから推定してください。",
      sep = "\n"
    )
  ),
  list(
    title = "信頼区間の半幅 E（精度ベース）",
    body = paste(
      "推定値の精度を「この幅で推定したい」という目標です。",
      "たとえば「95% CI の半幅 2」なら、平均値 ± 2 の幅で推定できる",
      "ことを目指します。",
      "検出や非劣性の判定は行わず、単に精度良く推定したい場合に使います。",
      "E を小さくすると必要症例数は二乗で増えるので、",
      "現実的な値を設定することが重要です。",
      sep = "\n"
    )
  ),
  # ===== 新規デザイン向け用語 =====
  list(
    title = "ハザード比 HR（生存時間解析）",
    body = paste(
      "ある時点で「まだイベント（死亡・増悪など）を起こしていない人」が",
      "次の瞬間にイベントを起こす瞬間速度（ハザード）の比です。",
      "HR < 1 なら治療が対照より良い、HR > 1 なら悪いと解釈します。",
      "log-rank 検定では、HR が 1 から離れているほど少ないイベント数で",
      "差を検出できます。生存曲線全体で比例ハザード仮定が成立する前提です。",
      sep = "\n"
    )
  ),
  list(
    title = "中央生存期間、必要イベント数と必要症例数",
    body = paste(
      "中央生存期間: 集団の半分がまだ生存している時点までの期間。",
      "指数分布仮定のとき、ハザード率 λ = log(2) / 中央生存期間。",
      "",
      "log-rank 検定で「検出に必要なのは症例数ではなくイベント数」です。",
      "必要イベント数 D は Schoenfeld (1981) の公式で決まり、",
      "症例数 N は D をイベント確率 q で割った値（N = D / q）として",
      "アクルー期間と追跡期間から計算されます。",
      "つまり速くイベントが起こるか、追跡を長くしないと、症例数は膨らみます。",
      sep = "\n"
    )
  ),
  list(
    title = "アクルー期間 a、フォローアップ期間 f",
    body = paste(
      "アクルー期間 a: 症例を登録していく期間です（例: 24 か月）。",
      "このツールでは rectangular accrual（均等にリクルート）を仮定します。",
      "追加フォローアップ期間 f: 最後の登録から観察を続ける期間です。",
      "総観察期間は a + f（最初に登録された人はこの全期間を観察される）。",
      "a と f を長くするほどイベント確率 q は上がり、必要症例数は減ります。",
      sep = "\n"
    )
  ),
  list(
    title = "共変量調整 ANCOVA",
    body = paste(
      "連続量アウトカムを、関連する共変量（ベースライン値など）で調整して",
      "評価する解析モデルです。共変量との相関 r が高いほど、",
      "調整後の残差の分散が (1 − r²) 倍に縮まり、必要症例数も (1 − r²) 倍に",
      "減ります（Borm 2007）。",
      "例: ベースライン HbA1c と治療後 HbA1c の相関が 0.7 なら、",
      "調整ありなら調整なしの約 51%（= 1 − 0.49）の症例数で同じ検出力を達成。",
      sep = "\n"
    )
  ),
  list(
    title = "同一被験者内の相関 ρ（反復測定）",
    body = paste(
      "同じ被験者における複数時点の測定値の類似度です。",
      "0 なら各時点は独立、1 なら全時点が完全一致。",
      "複数時点の平均を比較するとき、ρ が小さいほど「実質的に k 回",
      "別々に測った」ことになり、必要症例数は通常の 1/k に近づきます。",
      "ρ が大きい（各被験者の傾向が強い）と k 回測っても情報は 1 回分に",
      "近く、必要症例数はあまり減りません。",
      sep = "\n"
    )
  ),
  list(
    title = "級内相関 ICC、クラスターサイズ、Design Effect",
    body = paste(
      "クラスター（例: 病院、学級）内の被験者アウトカムはしばしば",
      "互いに似ています。その類似度が級内相関 ICC（0〜1）で、",
      "通常 0.01〜0.10 が報告されます。",
      "",
      "クラスターサイズ m と ICC から Design Effect DE = 1 + (m − 1)ICC。",
      "これは「独立個人を標本とする場合」より必要症例数が何倍必要かを表します。",
      "たとえば m = 20、ICC = 0.05 なら DE = 1.95 → 約 2 倍の症例数が必要。",
      sep = "\n"
    )
  ),
  list(
    title = "α spending（群逐次デザイン）",
    body = paste(
      "試験期間中に中間解析で検定を繰り返すと、単純には全体 α が",
      "膨らんでしまいます。これを抑えるため、解析回数 K にわたって",
      "α を「分配」するルールを決めます。",
      "",
      "O'Brien-Fleming: 早期の解析では境界を非常に厳しくし、",
      "  最終に α の大部分を割り振る（検証試験で標準的）。",
      "Pocock: 各解析で同じ（緩い）境界を使う。早期中止しやすいが、",
      "  全体の必要症例数が増える（10〜25%）。",
      "",
      "いずれも、固定デザインの必要症例数を inflation factor で",
      "膨らませるだけで概算できます（Jennison & Turnbull 2000）。",
      sep = "\n"
    )
  ),
  list(
    title = "感度、特異度、有病率（診断精度）",
    body = paste(
      "感度 Se: 疾患ありの人のうち、検査が陽性となる割合。",
      "特異度 Sp: 疾患なしの人のうち、検査が陰性となる割合。",
      "有病率 prev: 対象集団で疾患を持つ人の割合。",
      "",
      "診断精度試験では、Se と Sp それぞれを「望ましい信頼区間の半幅」で",
      "推定するための症例数を計算します。",
      "有病率が低いほど疾患あり例を集めるのが難しく、総必要例数が増えます。",
      sep = "\n"
    )
  ),
  list(
    title = "不一致ペア（McNemar）",
    body = paste(
      "対応のある二値データで、2 つの条件（前後 / A・B）で結果が",
      "異なるペアのことです。McNemar 検定は「一致しているペア」は",
      "ほぼ情報を持たず、不一致ペアの数とその偏り（ψ）だけで",
      "p 値を計算します。",
      "",
      "p_disc = 不一致ペアの割合（小さいほど必要症例数が増える）、",
      "ψ = 不一致のうち A 優位の割合（0.5 から離れるほど検出しやすい）。",
      sep = "\n"
    )
  ),
  list(
    title = "パラメトリックとノンパラメトリック",
    body = paste(
      "パラメトリック検定（t 検定など）: データが特定の分布（正規分布など）",
      "に従う前提で計算する検定。前提が成立すれば効率が高い。",
      "",
      "ノンパラメトリック検定（Mann-Whitney U など）: 分布形状を仮定せず、",
      "順位情報だけを使う検定。外れ値や歪んだ分布に強い。",
      "",
      "データが正規でないときは必要症例数が変わります。漸近相対効率",
      "ARE を使って、t 検定の必要症例数を調整すると概算できます。",
      "分布が裾の重いもの（指数分布など）では MW のほうが少ない症例数で",
      "同じ検出力を達成できます。",
      sep = "\n"
    )
  )
)

# =========================================================================
# デザインの選び方
# =========================================================================
design_selector_text <- list(
  title = "デザインの選び方",
  body = paste(
    "次のフローで選びます:",
    "",
    "● アウトカムの型で分ける",
    "  ・連続量（血圧、HbA1c、検査値など）→ 「連続量アウトカム」タブ",
    "  ・2 値（イベント有/無、成功/失敗）→ 「二値アウトカム」タブ",
    "  ・生存時間（時間 + イベント有無）→ 「生存時間解析」タブ",
    "  ・診断検査の評価（Se/Sp/PPV/NPV）→ 「診断精度」タブ",
    "",
    "● 目的で分ける",
    "  ・既存治療より「優れていること」を示す → 優越性（このツールの既定）",
    "  ・既存治療と「差がないこと」を示す → 「非劣性」（ラジオ切替）",
    "  ・検定はせず推定の精度だけが目的 → 「1 標本（精度ベース）」タブ",
    "",
    "● 測定の仕方で分ける",
    "  ・異なる 2 群を比較 → 2 標本",
    "  ・同じ人の前後比較（またはマッチドペア）→ 対応のある t 検定",
    "  ・前後比較の二値 → McNemar（二値アウトカムの「対応のある」）",
    "",
    "● 二値アウトカムの検定方法",
    "  ・事象が中程度の頻度（5%〜95%）→ χ² 検定",
    "  ・希少事象または少数例 → Fisher の正確検定",
    "  （このツールではどちらも pwr.2p.test ベースで近似します）",
    sep = "\n"
  )
)

# =========================================================================
# 典型的な研究計画の例（ケーススタディ）
# =========================================================================
case_study_text <- list(
  title = "典型的な研究計画の例",
  body = paste(
    "■ 新規降圧薬 vs プラセボ（2 群比較・連続量）",
    "  主要評価項目: 12 週後の収縮期血圧",
    "  想定: 降圧薬 −8 mmHg、プラセボ −2 mmHg、共通 SD 10、",
    "        両側 α = 0.05、検出力 80%、最終的な脱落割合 10%",
    "  → 「連続量 > 2 群比較（群別入力）」または「差と群別 SD」",
    "",
    "■ 既存経口薬 vs 新規注射薬（2 群比較・二値、非劣性）",
    "  主要評価項目: 8 週後の有効性達成割合",
    "  想定: 既存 60%、新規 58%、NI マージン 10%、片側 α = 0.025",
    "  → 「二値 > 2 群比較（χ²）」＋試験デザイン=非劣性",
    "",
    "■ 同一患者の治療前後 QOL スコア（対応のある連続量）",
    "  主要評価項目: 治療前後の QOL 差",
    "  想定: 差の平均 5 点、差の SD 8、α = 0.05、検出力 80%",
    "  → 「連続量 > 対応のある t 検定」",
    "",
    "■ 新規抗糖尿病薬の推定（1 標本・精度ベース）",
    "  目的: 治療後 HbA1c 低下量を 95% CI 半幅 0.3% で推定",
    "  想定: SD 1.5%、最終的な脱落割合 10%",
    "  → 「1 標本 > 平均（精度ベース）」",
    "",
    "■ 新規抗がん剤の OS（生存時間・優越性）",
    "  主要評価項目: 全生存期間 (OS)",
    "  想定: 対照中央 12 か月、HR 0.70、アクルー 24 か月、FU 18 か月、",
    "        片側 α = 0.025、検出力 80%",
    "  → 「生存時間解析 > log-rank 検定」",
    "",
    "■ 患者前後での診断一致（McNemar・対応あり二値）",
    "  主要評価項目: 新検査と旧検査の陽性判定の食い違い",
    "  想定: 不一致ペア割合 30%、そのうち新検査優位 70%、α = 0.05",
    "  → 「二値 > 対応のある（McNemar）」",
    "",
    "■ 新規 POCT の精度評価（診断精度）",
    "  目的: 感度と特異度を 95% CI 半幅 0.05 で推定",
    "  想定: 想定 Se = 0.90、Sp = 0.85、有病率 15%",
    "  → 「診断精度」タブ",
    sep = "\n"
  )
)

# =========================================================================
# よくある質問（FAQ）
# =========================================================================
faq_text <- list(
  title = "よくある質問 (FAQ)",
  body = paste(
    "Q: なぜ「解析対象」と「登録必要」で例数が違うのですか?",
    "A: 脱落する人を見込んで多めに登録する必要があります。",
    "   最終的な脱落割合 10% なら、解析対象 100 例のためには 112 例を登録します",
    "   （= ⌈100 / 0.90⌉）。",
    "",
    "Q: 検出力はいくつに設定すべきですか?",
    "A: 通常 80%。重要な承認試験や、見逃しの損失が大きい場合は 90%。",
    "",
    "Q: 生存時間解析で「必要イベント数」が出ますが、どういう意味ですか?",
    "A: log-rank 検定は、観察期間中に起こったイベント数で効果を検出します。",
    "   そのため必要イベント数が第一で、必要症例数はアクルー期間・FU 期間から",
    "   「この症例数を観察すれば必要イベント数が得られる」として計算されます。",
    "",
    "Q: 非劣性マージン M はどう決めますか?",
    "A: 臨床的妥当性 + 規制的観点（既存治療がプラセボに対して示した差の",
    "   一定割合、例えば 50% を超えないように）で事前に決めます。",
    "   試験結果に応じて後から変更するのは不適切です。",
    "",
    "Q: 本ツールで対応していないデザインはどうすれば良いですか?",
    "A: 複雑な多群・多時点比較、適応デザイン、多層的なクラスター構造、",
    "   希少疾患のような特殊な状況は、本ツールの範囲外です。",
    "   所属機関の生物統計家または臨床研究支援部門にご相談ください。",
    sep = "\n"
  )
)

# =========================================================================
# 結果の読み方
# =========================================================================
result_reading_text <- list(
  title = "結果の読み方",
  body = paste(
    "● 解析対象として必要な症例数 vs 登録必要症例数",
    "  ・解析対象: 最後まで追跡できて解析に使える人数",
    "  ・登録必要数: 脱落を見込んで最初に集めるべき人数（多め）",
    "",
    "● 感度分析グラフ（「グラフ」タブ）",
    "  ・横軸: 動かす変数（効果量、α、SD など）",
    "  ・縦軸: 検出力または必要症例数",
    "  ・凡例（色分け）: もう 1 つの変数を 4 値で比較",
    "  ・青い点線: 目標検出力または現在の必要症例数",
    "  ・赤い点線: 現在の入力値",
    "  実際の差が想定より小さい場合にどう振舞うかを",
    "  「横軸を少し悲観的に動かして」確認してください。",
    "",
    "● 論文記載用テキストの使い方（「結果」タブ下部）",
    "  ・そのまま貼らず、[] で示された箇所（アウトカム名、",
    "    参考文献など）を必ず埋めてください。",
    "  ・引用するパッケージバージョンが論文掲載時の環境と一致する",
    "    か確認してください。",
    sep = "\n"
  )
)

# =========================================================================
# 結果パネル用「読み解きヒント」（デザイン別）
# calc_mode で sample_size / power_calc を切り替える。
# =========================================================================
result_hint_text <- function(design_id, params, result, power_target = 0.80,
                             calc_mode = "sample_size") {
  if (calc_mode == "power_calc") {
    return(.hint_power_calc(design_id, params, result))
  }
  pw_pct <- sprintf("%d", round(power_target * 100))
  drop_pct <- sprintf("%d", round(params$dropout * 100))

  common_tail <- paste(
    "",
    sprintf("脱落率 %s%% を見込んで登録数は増やしてありますが、", drop_pct),
    "実際の脱落率が想定を超える場合は再計算が必要です。",
    sep = "\n"
  )
  graph_tail <- paste(
    "もし実際の差が想定より小さい場合、同じ症例数では検出力が下がります。",
    "「グラフ」タブで、差を小さめに見積もったときの検出力を",
    "確認することを推奨します。",
    sep = "\n"
  )

  body <- switch(design_id,
    ttest_m1 = sprintf(
      paste(
        "この数字は、想定した差（介入群平均 %.2f と対照群平均 %.2f の差）が",
        "本当に存在するとき、%s%% の確率でそれを統計的に検出できる",
        "最小の症例数です。",
        sep = "\n"
      ),
      params$mean_A, params$mean_B, pw_pct
    ),
    ttest_m2 = sprintf(
      paste(
        "この数字は、想定した群間差 Δ = %.2f が本当に存在するとき、",
        "%s%% の確率でそれを統計的に検出できる最小の症例数です。",
        sep = "\n"
      ),
      params$diff, pw_pct
    ),
    paired = sprintf(
      paste(
        "この数字は、想定した差の平均 %.2f が本当に存在するとき、",
        "対応のある t 検定で %s%% の確率で有意と判定できる最小のペア数です。",
        sep = "\n"
      ),
      params$diff_mean, pw_pct
    ),
    paired_corr = sprintf(
      paste(
        "この数字は、介入前 %.2f・介入後 %.2f（相関係数 r = %.2f）から",
        "換算された差の SD = %.2f、差の平均 = %.2f のとき、",
        "対応のある t 検定で %s%% の確率で有意と判定できる",
        "最小のペア数です。",
        "r を小さめに見積もると差のばらつきが大きくなり、",
        "必要ペア数は増えます（保守的になります）。",
        sep = "\n"
      ),
      params$mean_1, params$mean_2, params$r,
      result$sd_diff, result$diff_mean, pw_pct
    ),
    binary_chisq = sprintf(
      paste(
        "この数字は、介入群 %d%%、対照群 %d%% の発生割合が本当にあるとき、",
        "χ² 検定で %s%% の確率で有意と判定できる最小の各群症例数です。",
        sep = "\n"
      ),
      round(params$p_A * 100), round(params$p_B * 100), pw_pct
    ),
    binary_fisher = sprintf(
      paste(
        "この数字は、Fisher の正確検定を想定した際の近似的な必要症例数です",
        "（χ² 近似を採用しているため、Fisher は若干保守的＝もう少し多めの",
        "人数が必要になる可能性があります）。",
        "希少事象（p が 0.05 未満または 0.95 超）ではシミュレーションに基づく",
        "検出力計算の併用を推奨します。",
        sep = "\n"
      )
    ),
    one_mean = sprintf(
      paste(
        "この数字は、%s%% 信頼区間の半幅が %s 以下になるような",
        "平均値の推定に必要な症例数です。",
        "検出力（有無の判定確率）という概念はありません。",
        sep = "\n"
      ),
      sprintf("%d", round(params$conf_level * 100)),
      format(params$half_width)
    ),
    one_prop = sprintf(
      paste(
        "この数字は、%s%% 信頼区間（%s）の半幅が %s 以下になるような",
        "割合推定に必要な症例数です。",
        "検出力の概念はありません。",
        sep = "\n"
      ),
      sprintf("%d", round(params$conf_level * 100)),
      switch(params$method, normal = "正規近似", wilson = "Wilson", exact = "Exact"),
      format(params$half_width)
    ),
    ttest_ni = sprintf(
      paste(
        "この数字は、非劣性マージン M = %.2f のもとで、",
        "片側 α = %s の検定で %s%% の確率で「非劣性」と判定できる",
        "最小の各群症例数です。",
        "M は平均差の単位で設定しています。",
        sep = "\n"
      ),
      params$margin, sprintf("%.3f", params$alpha), pw_pct
    ),
    ttest_m2_ni = sprintf(
      paste(
        "この数字は、群間差 Δ = %.2f と非劣性マージン M = %.2f のもとで、",
        "片側 α = %s の 2 標本 t 検定で %s%% の確率で「非劣性」と",
        "判定できる最小の各群症例数です。",
        sep = "\n"
      ),
      params$diff, params$margin, sprintf("%.3f", params$alpha), pw_pct
    ),
    paired_ni = sprintf(
      paste(
        "この数字は、マージン M = %.2f のもとで、対応のある片側 t 検定で",
        "%s%% の確率で「非劣性」と判定できる最小のペア数です。",
        sep = "\n"
      ),
      params$margin, pw_pct
    ),
    binary_ni = sprintf(
      paste(
        "この数字は、リスク差マージン M = %d%% のもとで、",
        "Chow ら (2018) の正規近似公式に基づいて %s%% の確率で",
        "「非劣性」と判定できる最小の各群症例数です。",
        "より正確な計算には Farrington-Manning 法の使用を推奨します。",
        sep = "\n"
      ),
      round(params$margin * 100), pw_pct
    ),
    mcnemar = sprintf(
      paste(
        "この数字は、不一致ペアの割合 p_disc = %.2f、",
        "そのうち A が優位な割合 ψ = %.2f のもとで、",
        "McNemar 検定（両側 α = %s）で %s%% の確率で有意と判定できる",
        "最小のペア数です。",
        "ψ が 0.5 に近いほど（A/B の優位が拮抗するほど）必要ペア数は急増します。",
        sep = "\n"
      ),
      params$p_disc, params$psi, sprintf("%.3f", params$alpha), pw_pct
    ),
    ancova = sprintf(
      paste(
        "この数字は、介入群平均 %.2f と 対照群平均 %.2f の差（共通 SD = %.2f）",
        "を、共変量との相関 r = %.2f で調整する ANCOVA モデルで",
        "%s%% の確率で有意と判定できる最小の各群症例数です。",
        "r が大きいほど分散低減効果で必要症例数は減ります。",
        sep = "\n"
      ),
      params$mean_A, params$mean_B, params$sd_common, params$r, pw_pct
    ),
    logrank = sprintf(
      paste(
        "この数字は、対照群の中央生存期間 %.1f、ハザード比 %.2f を仮定し、",
        "登録期間 %.1f・追跡期間 %.1f の指数分布 + 一様リクルートのもとで、",
        "%s%% の確率でハザード比の違いを log-rank 検定で検出するのに必要な",
        "「イベント数」を総症例数に換算したものです。",
        "イベント数が主要で、症例数は登録期間・追跡期間に強く依存します。",
        sep = "\n"
      ),
      params$median_C, result$HR %||% params$HR %||% NA_real_,
      params$accrual, params$followup, pw_pct
    ),
    longitudinal = sprintf(
      paste(
        "この数字は、%d 回測定の平均値の差を比較する前提で、被験者内相関",
        "ρ = %.2f を仮定したときに、%s%% の確率で有意と判定できる",
        "最小の各群症例数です。",
        "ρ が低く k が多いほど有効情報量が増え、必要症例数は減ります。",
        sep = "\n"
      ),
      as.integer(params$k), params$rho, pw_pct
    ),
    group_sequential = sprintf(
      paste(
        "この数字は、%s 境界を使った %d 回の解析（最終解析を含む）における",
        "群逐次デザインで、%s%% の確率で有意と判定できる最小の各群症例数です。",
        "OBF は最終解析に境界を強く配分するため固定デザインより 1〜3%% の",
        "増加で済み、Pocock は早期中止しやすい代わりに 10〜25%% 増えます。",
        sep = "\n"
      ),
      params$boundary, as.integer(params$K), pw_pct
    ),
    cluster_cont = sprintf(
      paste(
        "この数字は、平均クラスターサイズ %d、ICC = %.3f のもとでの",
        "Design Effect を適用した必要症例数です。",
        "%s%% の確率で有意と判定するには各群 %d クラスター必要で、",
        "実際の組入は丸めの関係で各群 %d 例となります。",
        "ICC と m のどちらかが小さいほど必要症例数は減ります。",
        sep = "\n"
      ),
      as.integer(params$m), params$ICC, pw_pct,
      as.integer(result$K_per_arm %||% NA), result$n_per_arm_evaluable
    ),
    cluster_bin = sprintf(
      paste(
        "この数字は、平均クラスターサイズ %d、ICC = %.3f のもとでの",
        "Design Effect を適用した必要症例数です（二値アウトカム）。",
        "%s%% の確率で有意と判定するには各群 %d クラスター必要で、",
        "実際の組入は各群 %d 例となります。",
        sep = "\n"
      ),
      as.integer(params$m), params$ICC, pw_pct,
      as.integer(result$K_per_arm %||% NA), result$n_per_arm_evaluable
    ),
    diagnostic = sprintf(
      paste(
        "この数字は、感度 %s%%・特異度 %s%%・有病率 %s%% を仮定したとき、",
        "感度と特異度それぞれの %s%% 信頼区間の半幅が %s 以下になる",
        "ために必要な総例数です（疾患あり %d 例、疾患なし %d 例の要件",
        "を同時に満たす）。",
        "検出力という概念はなく、CI 幅の目標のみを用います。",
        sep = "\n"
      ),
      round(params$Se * 100), round(params$Sp * 100),
      round(params$prev * 100),
      sprintf("%d", round(params$conf_level * 100)),
      format(params$half_width),
      result$n_dis_required %||% 0L,
      result$n_non_required %||% 0L
    ),
    mann_whitney = sprintf(
      paste(
        "この数字は、介入群 %.2f と 対照群 %.2f（共通 SD %.2f）の位置パラメータの",
        "差を Mann-Whitney U 検定で検出するのに、%s%% の確率で有意と判定",
        "できる最小の各群症例数です。",
        "分布の仮定（正規 / 対数正規 / 指数）によって漸近相対効率 ARE が",
        "変わり、裾の重い分布では t 検定より必要例数が少なくなります。",
        sep = "\n"
      ),
      params$mean_A, params$mean_B, params$sd_common, pw_pct
    ),
    "（デザイン別のヒントは未定義です）"
  )

  has_graph <- !(design_id %in% c("one_mean", "one_prop", "diagnostic"))
  paste(body, if (has_graph) graph_tail else NULL, common_tail, sep = "\n")
}

# 検出力モードのヒント（各デザイン）
.hint_power_calc <- function(design_id, params, result) {
  pw_pct <- sprintf("%.1f", (result$achieved_power %||% 0) * 100)
  n <- params$n
  tail <- paste(
    "",
    "検出力が目標（通常 80% や 90%）に足りないときは、",
    "サイドバー上部の「計算モード」を「必要症例数を計算する」に切替えて",
    "必要 n を見積もってください。",
    sep = "\n"
  )
  body <- switch(design_id,
    ttest_m1 = sprintf(paste(
        "各群 %d 例の条件で、想定した差（介入群平均 %.2f・対照群平均 %.2f）を",
        "統計的に有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$mean_A, params$mean_B, pw_pct),
    ttest_m2 = sprintf(paste(
        "各群 %d 例の条件で、想定した群間差 Δ = %.2f を",
        "統計的に有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$diff, pw_pct),
    paired = sprintf(paste(
        "%d ペアの条件で、想定した差の平均 %.2f を",
        "対応のある t 検定で有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$diff_mean, pw_pct),
    paired_corr = sprintf(paste(
        "%d ペアの条件で、治療前 %.2f・治療後 %.2f・相関係数 %.2f",
        "（換算された差の SD = %.2f）から想定される差を",
        "対応のある t 検定で有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$mean_1, params$mean_2, params$r,
      result$sd_diff %||% NA_real_, pw_pct),
    binary_chisq = sprintf(paste(
        "各群 %d 例の条件で、介入群 %d%%・対照群 %d%% の発生割合の差を",
        "χ² 検定で有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, round(params$p_A * 100), round(params$p_B * 100), pw_pct),
    binary_fisher = sprintf(paste(
        "各群 %d 例の条件で、介入群 %d%%・対照群 %d%% の発生割合の差を",
        "Fisher の正確検定（χ² 近似）で有意と判定できる確率は",
        "%s%% と計算されました。",
        sep = "\n"),
      n, round(params$p_A * 100), round(params$p_B * 100), pw_pct),
    ttest_ni = sprintf(paste(
        "各群 %d 例の条件で、非劣性マージン M = %.2f のもとで",
        "片側 α = %s で非劣性と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$margin, sprintf("%.3f", params$alpha), pw_pct),
    ttest_m2_ni = sprintf(paste(
        "各群 %d 例の条件で、群間差 Δ = %.2f・マージン M = %.2f のもとで",
        "片側 α = %s で非劣性と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$diff, params$margin, sprintf("%.3f", params$alpha), pw_pct),
    paired_ni = sprintf(paste(
        "%d ペアの条件で、マージン M = %.2f のもとで",
        "非劣性と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$margin, pw_pct),
    binary_ni = sprintf(paste(
        "各群 %d 例の条件で、リスク差マージン M = %d%% のもとで",
        "非劣性と判定できる確率は %s%% と計算されました",
        "（Chow 2018 正規近似ベース）。",
        sep = "\n"),
      n, round(params$margin * 100), pw_pct),
    mcnemar = sprintf(paste(
        "%d ペアの条件で、p_disc = %.2f, ψ = %.2f のもとで McNemar 検定で",
        "有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$p_disc, params$psi, pw_pct),
    ancova = sprintf(paste(
        "各群 %d 例の条件で、相関 r = %.2f の共変量調整 ANCOVA で",
        "有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$r, pw_pct),
    logrank = sprintf(paste(
        "総症例数 %d 例の条件で、HR = %.2f・対照群の中央生存期間 %.1f・",
        "登録期間 %.1f・追跡期間 %.1f のもとで log-rank 検定で",
        "有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, result$HR %||% params$HR %||% NA_real_, params$median_C,
      params$accrual, params$followup, pw_pct),
    longitudinal = sprintf(paste(
        "各群 %d 例、k = %d、ρ = %.2f の条件で、反復測定の平均比較で",
        "有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, as.integer(params$k), params$rho, pw_pct),
    group_sequential = sprintf(paste(
        "各群 %d 例の条件で、%s 境界・K = %d の群逐次デザインで",
        "有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$boundary, as.integer(params$K), pw_pct),
    cluster_cont = sprintf(paste(
        "各群 %d 例（m = %d, ICC = %.3f）の条件で、クラスターランダム化",
        "連続量アウトカムの有意判定確率は %s%% と計算されました。",
        sep = "\n"),
      n, as.integer(params$m), params$ICC, pw_pct),
    cluster_bin = sprintf(paste(
        "各群 %d 例（m = %d, ICC = %.3f）の条件で、クラスターランダム化",
        "二値アウトカムの有意判定確率は %s%% と計算されました。",
        sep = "\n"),
      n, as.integer(params$m), params$ICC, pw_pct),
    mann_whitney = sprintf(paste(
        "各群 %d 例（分布仮定: %s）の条件で、Mann-Whitney U 検定で",
        "有意と判定できる確率は %s%% と計算されました。",
        sep = "\n"),
      n, params$distribution %||% "normal", pw_pct),
    "（検出力モードでのヒントは未定義です）"
  )
  paste(body, tail, sep = "\n")
}
